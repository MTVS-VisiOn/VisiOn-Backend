# Docker 는 .gitignore 를 보지 않는다. 여기 없는 파일은 전부 빌드 컨텍스트에 실린다.

# 로컬 빌드 산출물 — 컨테이너 안에서 다시 빌드하므로 보낼 필요 없다
build/
.gradle/

# 비밀값
.env
.env.dev
.env.prod
.env.test

# 수동 테스트 페이지.
# SecurityConfig 가 .anyRequest().permitAll() 로 끝나므로 이미지에 들어가면 배포 환경에서 전부 공개된다.
src/main/resources/static/signal-test.html

.git/
.github/
.idea/
*.iml
HELP.md
